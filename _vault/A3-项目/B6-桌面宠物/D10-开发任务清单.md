---
title: "灵伴桌面宠物 — 开发任务清单 (v2.0 P0)"
date: 2026-08-05 14:22:31 +0800
categories: [桌面宠物]
tags: [桌面宠物, 脚本任务管理]
pin: true
published: true
author: 
    name: hejiahua007
    link: https://space.bilibili.com/507838758
toc: true
comments: true
math: false
mermaid: true
---

# 灵伴桌面宠物 — 开发任务清单 (v2.0 P0)

> **日期**: 2026-08-05  
> **执行顺序**: 严格按编号顺序执行（先清理，再开发）  
> **工时单位**: 人天（1 天 = 8 小时）

---

> **审查更新 (2026-08-05)**: 根据 team-lead 审查意见，已做以下调整：
> - 阶段四新增 T-018.5: Steam SDK 技术预研任务
> - T-020 云存档工时从 1 天缩减到 0.3 天
> - 阶段二 T-011/T-012/T-014 标注为简化版，完整版推迟到 v2.1
> - 阶段二工时从 12.5 天压缩到 8 天
> - 总工时从 36 天调整为 32.8 天

---

## 阶段一：技术债务清理 (预计 3 天)

### T-001: 删除 Wallpaper Engine 硬编码

| 属性 | 内容 |
|---|---|
| **标题** | 删除 config-store.js 中 Wallpaper Engine 硬编码 |
| **描述** | 删除 `WALLPAPER_ENGINE` 常量、`wallpaperScript()` 函数，为后续通用模板重写做准备 |
| **涉及文件** | `src/config-store.js` (L7, L9-51) |
| **预估工时** | 0.5 天 |
| **验收标准** | 代码库中无 `WALLPAPER_ENGINE`、`wallpaperScript` 引用；`npm run check` 通过 |
| **依赖** | 无 |

### T-002: 重写内置任务模板

| 属性 | 内容 |
|---|---|
| **标题** | 将 3 个 WE 模板替换为 7 个通用模板 |
| **描述** | 重写 `createDefaultTasks()` 函数，生成 7 个面向普通用户的通用提醒模板（晨间唤醒、喝水提醒、午休提醒、眼保健操、每日总结、晚间放松、定时关机），全部默认关闭 |
| **涉及文件** | `src/config-store.js` (L53-89), `tests/integration.js` (L15) |
| **预估工时** | 1 天 |
| **验收标准** | 7 个模板任务正确生成；每个任务 `enabled: false`；脚本内容为通用 PowerShell 通知代码，无 Wallpaper Engine 引用；集成测试断言更新 |
| **依赖** | T-001 |

### T-003: 删除旧任务迁移系统 (主进程)

| 属性 | 内容 |
|---|---|
| **标题** | 删除 main.js 中的 adoptLegacyTasks 函数及 IPC 通道 |
| **描述** | 删除 `adoptLegacyTasks()` 函数 (L450-469) 和 `registerHandler('legacy:adopt', ...)` (L538)。该迁移系统为 v1.0 用户从 Windows 计划任务迁移到应用内模板所用，v2.0 面向新用户不需要 |
| **涉及文件** | `src/main.js` (L450-469, L538) |
| **预估工时** | 0.5 天 |
| **验收标准** | `adoptLegacyTasks` 函数完全移除；`legacy:adopt` IPC 通道移除；`npm run check` 通过 |
| **依赖** | T-001 |

### T-004: 删除旧任务迁移系统 (预加载)

| 属性 | 内容 |
|---|---|
| **标题** | 删除 preload.js 中 adoptLegacyTasks API |
| **描述** | 删除 `contextBridge` 中暴露的 `adoptLegacyTasks` 方法 (L29) |
| **涉及文件** | `src/preload.js` (L29) |
| **预估工时** | 0.1 天 |
| **验收标准** | `lingPet.adoptLegacyTasks` 不再存在于 API 中 |
| **依赖** | T-003 |

### T-005: 删除旧任务迁移系统 (渲染进程)

| 属性 | 内容 |
|---|---|
| **标题** | 删除 manager.html 和 manager.js 中的迁移 UI |
| **描述** | 删除 `.migration-card` HTML 面板 (L56-61)；删除 manager.js 中所有 `adoptLegacy`、`migrationStatus` 相关代码 (L15-16, L114-117, L173-186) |
| **涉及文件** | `src/manager.html` (L56-61), `src/manager.js` (L15-16, L114-117, L173-186) |
| **预估工时** | 0.4 天 |
| **验收标准** | 控制中心无"旧任务迁移"面板；`legacyTasksAdopted` 设置项从 config-store.js 默认值中移除 (L118) |
| **依赖** | T-004 |

### T-006: 更新集成测试

| 属性 | 内容 |
|---|---|
| **标题** | 适配新的默认任务模板到集成测试 |
| **描述** | 将 `tests/integration.js` L15 的断言从 `tasks.length === 3` 改为 `tasks.length === 7` |
| **涉及文件** | `tests/integration.js` (L15) |
| **预估工时** | 0.2 天 |
| **验收标准** | `npm test` 全部通过 |
| **依赖** | T-002 |

### T-007: 代码库全局清理验证

| 属性 | 内容 |
|---|---|
| **标题** | 搜索确认无残留的一次性代码 |
| **描述** | 全局搜索 `wallpaper`, `WALLPAPER_ENGINE`, `adoptLegacy`, `legacyTasksAdopted`, `D:\\Games`, `Codex-` 等关键词，确认全部清除 |
| **涉及文件** | 全部 `src/`, `tests/` |
| **预估工时** | 0.3 天 |
| **验收标准** | 所有搜索关键词无匹配结果；`npm run check` + `npm test` + `npm start` 全部通过 |
| **依赖** | T-001 ~ T-006 |

---

## 阶段二：核心重构 (预计 8 天) [简化版]

### T-008: 引入国际化框架 (i18n)

| 属性 | 内容 |
|---|---|
| **标题** | 安装并配置 i18next 国际化框架 |
| **描述** | 安装 `i18next` + `i18next-fs-backend`；创建 `locales/zh-CN.json` 和 `locales/en-US.json` 语言文件；在渲染进程中初始化 i18n 实例；通过 preload 暴露 `getLanguage()` / `setLanguage()` API |
| **涉及文件** | `package.json`, `src/preload.js`, `src/main.js`, `src/pet.js`, `src/manager.js` |
| **预估工时** | 1 天 |
| **验收标准** | i18n 框架正确初始化；中英文切换正常；所有硬编码 UI 文本标记为待迁移 |
| **依赖** | T-007 |

### T-009: 提取所有 UI 文本到语言文件

| 属性 | 内容 |
|---|---|
| **标题** | 将所有 UI 文本从代码中提取到 JSON 语言文件 |
| **描述** | 将 `pet.html`, `manager.html` 中的所有中文硬编码文本、JS 中的 toast 消息、通知文本、任务模板描述等全部提取到 `zh-CN.json` 和 `en-US.json` |
| **涉及文件** | `src/pet.html`, `src/pet.js`, `src/manager.html`, `src/manager.js`, `locales/zh-CN.json`, `locales/en-US.json` |
| **预估工时** | 2 天 |
| **验收标准** | 所有用户可见文本通过 `t()` 函数获取；中英文切换后 UI 文本正确显示；无硬编码中文字符串（代码注释除外） |
| **依赖** | T-008 |

### T-010: 控制中心 UI 重设计 — 布局重构

| 属性 | 内容 |
|---|---|
| **标题** | 重构控制中心为 Dashboard + 左侧导航布局 |
| **描述** | 按照 `07-UI-UX改版方案.md` 的设计，将控制中心从"左侧设置+右侧任务"双栏布局重构为"左侧导航+右侧内容区"布局。新增导航：概览(Dashboard)、宠物、提醒、外观、设置 |
| **涉及文件** | `src/manager.html`, `src/manager.css` |
| **预估工时** | 2 天 |
| **验收标准** | 5 个导航入口可切换；布局响应式支持 900px+ 宽度；导航高亮当前页面 |
| **依赖** | T-009 |

### T-011: 控制中心 UI 重设计 — Dashboard 页面 [简化版]

| 属性 | 内容 |
|---|---|
| **标题** | 实现概览 Dashboard 页面（简化版） |
| **描述** | 实现"今日提醒"卡片（显示今日已完成/总数、下一个提醒）、"快捷操作"面板。宠物状态卡片简化，v2.1 完善。 |
| **涉及文件** | `src/manager.html`, `src/manager.css`, `src/manager.js` |
| **预估工时** | 1 天 |
| **验收标准** | Dashboard 卡片数据实时更新；点击快捷操作正确跳转 |
| **依赖** | T-010 |
| **备注** | 完整版（含宠物状态、心情系统）推迟到 v2.1 |

### T-012: 控制中心 UI 重设计 — 宠物页面 [简化版]

| 属性 | 内容 |
|---|---|
| **标题** | 宠物管理页面（简化版：保留现有功能 + 换皮） |
| **描述** | 将现有宠物设置功能迁移到新布局中，仅改 CSS 变量和布局框架，不重新实现交互逻辑。宠物列表（多宠物预留）仅做 UI 占位 |
| **涉及文件** | `src/manager.html`, `src/manager.css`, `src/manager.js` |
| **预估工时** | 0.5 天 |
| **验收标准** | 宠物预览正常；更换形象/恢复默认功能正常；大小滑块正常 |
| **依赖** | T-010 |
| **备注** | 完整版（含多宠物、形象选择器标签页）推迟到 v2.1

### T-013: 控制中心 UI 重设计 — 提醒页面

| 属性 | 内容 |
|---|---|
| **标题** | 实现提醒管理页面 |
| **描述** | 重构任务列表为提醒列表（卡片式）、新增"从模板添加"入口、将 PowerShell 编辑器折叠到"高级"面板中默认隐藏、新增频率选择（每天一次/每小时/自定义）、新增执行历史内嵌展示 |
| **涉及文件** | `src/manager.html`, `src/manager.css`, `src/manager.js` |
| **预估工时** | 2 天 |
| **验收标准** | 提醒列表正常渲染；从模板添加功能可用；高级面板折叠/展开正常；新建/编辑/删除/运行提醒正常 |
| **依赖** | T-010 |

### T-014: 控制中心 UI 重设计 — 外观与设置页面 [简化版]

| 属性 | 内容 |
|---|---|
| **标题** | 外观和设置页面（简化版：保留现有功能 + 换皮） |
| **描述** | 将现有设置功能（开机自启、总在最前、动画、大小）迁移到新布局中；外观页面预留主题切换 UI 占位（v2.1 实现）；语言切换和数据管理正常实现 |
| **涉及文件** | `src/manager.html`, `src/manager.css`, `src/manager.js` |
| **预估工时** | 0.5 天 |
| **验收标准** | 设置项持久化；语言切换正常；数据导出/导入正常 |
| **依赖** | T-010 |
| **备注** | 完整主题系统推迟到 v2.1

---

## 阶段三：首次体验优化 (预计 4 天)

### T-015: 首次启动引导向导 — HTML/CSS

| 属性 | 内容 |
|---|---|
| **标题** | 实现引导向导的页面结构和样式 |
| **描述** | 创建 `src/wizard.html` + `src/wizard.css`，实现 5 步引导流程：欢迎 → 语言选择 → 宠物选择（MVP 仅 2-3 只可选，后续设计师补充至 6 只）→ 提醒设置 → 完成。每步有进度指示器 |
| **备注** | MVP 阶段宠物形象仅有现有 `assets/pet.png`，可考虑简单颜色变体或剪影占位。完整 6 只宠物形象由设计师在 v2.1 提供。 |
| **涉及文件** | `src/wizard.html`, `src/wizard.css` (新建) |
| **预估工时** | 1.5 天 |
| **验收标准** | 5 步流程视觉正确；进度指示器正常；响应式布局 |
| **依赖** | T-009 |

### T-016: 首次启动引导向导 — JS 逻辑

| 属性 | 内容 |
|---|---|
| **标题** | 实现引导向导的交互逻辑 |
| **描述** | 创建 `src/wizard.js`，实现步骤切换、宠物选择（预览+确认）、提醒模板勾选、最终保存并跳转到主界面 |
| **涉及文件** | `src/wizard.js` (新建), `src/main.js` (新增 wizard 窗口创建) |
| **预估工时** | 1.5 天 |
| **验收标准** | 向导完整走通；选择结果正确保存；完成后正常进入宠物窗口 |
| **依赖** | T-015 |

### T-017: 首次启动检测与触发

| 属性 | 内容 |
|---|---|
| **标题** | 实现首次启动检测逻辑 |
| **描述** | 在 config-store.js 中增加 `onboardingCompleted` 标志；main.js 启动时检测该标志，未完成则打开引导向导而非宠物窗口；引导完成后设置标志并启动正常流程 |
| **涉及文件** | `src/config-store.js`, `src/main.js` |
| **预估工时** | 0.5 天 |
| **验收标准** | 首次启动进入引导向导；再次启动直接显示宠物；重置配置后再次触发向导 |
| **依赖** | T-016 |

### T-018: 宠物窗口交互优化

| 属性 | 内容 |
|---|---|
| **标题** | 优化宠物窗口的交互提示和视觉反馈 |
| **描述** | 将单个悬浮按钮改为三个常驻按钮（互动/设置/更多）；增加悬停 tooltip（2秒后显示操作提示）；任务执行时显示气泡通知；右键菜单精简（移除开发者术语） |
| **涉及文件** | `src/pet.html`, `src/pet.css`, `src/pet.js`, `src/main.js` (右键菜单重构) |
| **预估工时** | 1 天 |
| **验收标准** | 三按钮始终可见；tooltip 正常显示/隐藏；气泡动画流畅；右键菜单用户友好 |
| **依赖** | T-009 |

---

## 阶段四：Steam 集成 (预计 9 天)

### T-018.5: Steam SDK 技术预研 [新增]

| 属性 | 内容 |
|---|---|
| **标题** | Steamworks SDK 技术方案预研 |
| **描述** | 调研三种方案对 Electron 43 的兼容性：(a) greenworks (b) steamworks.js (c) 直接通过子进程调用 Steam API。输出选定方案 + PoC 代码。 |
| **涉及文件** | `src/steam-poc.js` (临时，预研用) |
| **预估工时** | 1 天 |
| **验收标准** | 选定方案能在 Electron 43 中成功初始化 Steam API；能获取用户 Steam ID；确认 greenworks 与 Electron 43 的兼容性状态 |
| **依赖** | T-007 |
| **备注** | **关键阻塞点**：如果三种方案都不兼容，需重新评估 Steam 集成策略 |

### T-019: Steamworks SDK 集成

| 属性 | 内容 |
|---|---|
| **标题** | 集成 greenworks 实现 Steamworks API 调用 |
| **描述** | 安装 `greenworks` 依赖；配置 Steamworks App ID；在主进程中初始化 Steam API；处理 Steam 客户端启动/未启动场景 |
| **涉及文件** | `package.json`, `src/main.js`, `src/steam.js` (新建) |
| **预估工时** | 3 天 |
| **验收标准** | Steam 客户端运行时应用正常初始化 Steam API；Steam 未运行时应用降级运行（不崩溃） |
| **依赖** | T-018.5 |

### T-020: Steam 云存档

| 属性 | 内容 |
|---|---|
| **标题** | 配置 Steam Auto-Cloud 云存档 |
| **描述** | 通过 Steamworks Auto-Cloud 配置实现云存档（无需代码层面大量工作）；在 Steamworks 后台配置云存档路径指向 `config.json`；确保代码中配置文件路径符合 Auto-Cloud 规范；测试冲突场景 |
| **涉及文件** | Steamworks 后台配置, `src/config-store.js` (路径确认) |
| **预估工时** | 0.3 天 |
| **验收标准** | 不同设备间配置自动同步；存档冲突正确处理 |
| **依赖** | T-019 |

### T-021: Steam Overlay 配置

| 属性 | 内容 |
|---|---|
| **标题** | 配置 Steam Overlay 行为 |
| **描述** | 宠物窗口（透明无边框）排除 Overlay 注入；控制中心窗口允许 Overlay；处理 Overlay 激活/停用时的窗口行为 |
| **涉及文件** | `src/main.js` |
| **预估工时** | 0.5 天 |
| **验收标准** | 宠物窗口无 Overlay；控制中心可呼出 Overlay；Overlay 不影响宠物动画 |
| **依赖** | T-019 |

### T-022: Steam 成就系统

| 属性 | 内容 |
|---|---|
| **标题** | 实现 Steam 成就系统（基础版） |
| **描述** | 定义 15 个初始成就（首次设置宠物、创建第一个提醒、连续 7 天打卡、自定义宠物形象等）；在主进程中实现成就解锁逻辑；成就数据本地缓存 |
| **涉及文件** | `src/steam.js`, `src/main.js`, `src/config-store.js` |
| **预估工时** | 2 天 |
| **验收标准** | 满足条件时成就正确解锁；Steam 客户端显示成就弹窗；离线解锁的成就在下次 Steam 在线时同步 |
| **依赖** | T-019 |

### T-023: 安装包配置与签名

| 属性 | 内容 |
|---|---|
| **标题** | 配置 Steam 分发的构建与签名 |
| **描述** | 调整 electron-builder 配置适配 Steam 分发（非 NSIS）；配置代码签名证书；生成 Steam 上传用的构建产物 |
| **涉及文件** | `package.json`, `build/` |
| **预估工时** | 1.5 天 |
| **验收标准** | 生成符合 Steamworks 要求的构建产物；安装包通过 Steam 上传验证 |
| **依赖** | T-019 |

---

## 阶段五：测试与打磨 (预计 5 天)

### T-024: 单元测试补充

| 属性 | 内容 |
|---|---|
| **标题** | 为 config-store 和 task-scheduler 增加单元测试 |
| **描述** | 测试任务 CRUD、配置容错、调度逻辑、防重复机制、新增的通用模板 |
| **涉及文件** | `tests/unit/` (新建) |
| **预估工时** | 1.5 天 |
| **验收标准** | 核心模块测试覆盖率 ≥ 80% |
| **依赖** | T-002 |

### T-025: E2E 测试

| 属性 | 内容 |
|---|---|
| **标题** | 编写关键用户流程的 E2E 测试 |
| **描述** | 使用 Playwright 测试：首次启动引导完整流程、创建提醒流程、任务执行流程、设置变更持久化 |
| **涉及文件** | `tests/e2e/` (新建) |
| **预估工时** | 2 天 |
| **验收标准** | 4 个关键流程 E2E 测试通过 |
| **依赖** | T-017 |

### T-026: 性能测试与优化

| 属性 | 内容 |
|---|---|
| **标题** | 性能基准测试与优化 |
| **描述** | 测量 CPU/内存/启动时间；优化动画帧率；减少不必要的重渲染；优化 Electron 启动参数 |
| **涉及文件** | `src/main.js`, `src/pet.css`, `src/pet.js` |
| **预估工时** | 1 天 |
| **验收标准** | 空闲 CPU < 1%；内存 < 150MB；冷启动 < 3s (SSD)；动画稳定 60fps |
| **依赖** | T-018 |

### T-027: 兼容性测试

| 属性 | 内容 |
|---|---|
| **标题** | Windows 版本兼容性测试 |
| **描述** | 在 Windows 10 (21H2+)、Windows 11 各版本上测试；测试不同 DPI 缩放 (100%/125%/150%/200%)；测试多显示器环境 |
| **涉及文件** | 无 |
| **预估工时** | 0.5 天 |
| **验收标准** | 所有目标 Windows 版本正常运行；DPI 缩放正确；多显示器拖拽正常 |
| **依赖** | T-026 |

---

## 阶段六：发布准备 (预计 3 天)

### T-028: Steam 商店页面制作

| 属性 | 内容 |
|---|---|
| **标题** | 制作 Steam 商店页面所有资产 |
| **描述** | 商店文案（中英文）；5 张截图（含标注）；预告片脚本与录制；商店横幅/胶囊图；标签与分类设置 |
| **涉及文件** | `store-assets/` (新建) |
| **预估工时** | 2 天 |
| **验收标准** | 所有商店资产符合 Steamworks 规范；截图清晰展示核心功能 |
| **依赖** | T-018 |

### T-029: 发布检查清单

| 属性 | 内容 |
|---|---|
| **标题** | 执行发布前检查清单 |
| **描述** | 检查项包括：版本号更新、CHANGELOG 编写、许可协议确认、隐私政策（如有遥测）、Steamworks 审核材料、构建产物完整性 |
| **涉及文件** | `package.json`, `README.md`, `CHANGELOG.md` |
| **预估工时** | 0.5 天 |
| **验收标准** | 所有检查项通过 |
| **依赖** | T-027, T-028 |

### T-030: Steam 审核提交

| 属性 | 内容 |
|---|---|
| **标题** | 提交 Steam 商店页面和构建版本审核 |
| **描述** | 在 Steamworks 后台提交商店页面审核；上传构建版本并提交审核；准备审核申诉材料（如需要） |
| **涉及文件** | Steamworks 后台 |
| **预估工时** | 0.5 天 |
| **验收标准** | 商店页面和构建版本均已提交审核 |
| **依赖** | T-029 |

---

## 总工时汇总

| 阶段 | 任务数 | 预估工时 |
|---|---|---|
| 阶段一：技术债务清理 | 7 | 3.0 天 |
| 阶段二：核心重构 [简化版] | 7 | 8.0 天 |
| 阶段三：首次体验优化 | 4 | 4.5 天 |
| 阶段四：Steam 集成 | 6 | 9.0 天 |
| 阶段五：测试与打磨 | 4 | 5.0 天 |
| 阶段六：发布准备 | 3 | 3.0 天 |
| **合计** | **31** | **32.5 天** |

> 注：以上为单名全栈开发者预估。如有前端/后端分工可并行缩短周期。实际工时可能因技术难点（尤其是 Steam SDK 兼容性）上浮 20-30%。
>
> **审查调整项**：
> - T-018.5 新增 Steam SDK 预研 (1天)
> - T-020 云存档工时缩减 (1天→0.3天)
> - T-011/T-012/T-014 简化版，完整版推迟到 v2.1
> - 阶段二压缩 (12.5天→8天)
> - 净变化：36天→32.5天 (-3.5天)

---

## 关键依赖链

```
T-001 → T-002 → T-006 → T-007 (清理链)
                    ↓
T-008 → T-009 → T-010 → T-011~T-014 (UI链) [简化版]
                    ↓
              T-015 → T-016 → T-017 (向导链)
                    ↓
              T-018 (交互优化)
                    
T-007 → T-018.5 → T-019 → T-020~T-023 (Steam链) [含预研]

T-002 → T-024 (测试链)
T-017 → T-025
T-018 → T-026 → T-027

T-018 → T-028
T-027 → T-029 → T-030
```
